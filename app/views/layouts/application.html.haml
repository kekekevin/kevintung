!!!
%html
  %head
    %title kevintung
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body{class: "#{controller_name} #{action_name}"}
    .container-narrow
      .masthead
        %ul.nav.nav-pills.pull-right
          - { :home => "Home", :projects => "Projects", :connect => "Connect" }.each do |tab, tab_display|
            %li{ :class => (tab.to_s == @active_tab.to_s) ? "active" : "" }
              = link_to tab_display, static_path(tab) 
        %h3.muted
          kevintung
      %hr
      .container-fluid
        = yield

      %hr
      .footer
        %p.muted
          Copyright kevintung 2013. As if anyone reads this portion.
