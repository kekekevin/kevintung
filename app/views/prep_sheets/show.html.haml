%h1 And then we need to prep 

%h2
  Sheet:
  = @prep_sheet.par_sheet.name
  generated at
  = @prep_sheet.created_at.strftime "%l:%M:%S %p %m/%d/%Y %Z"
%legend I need...

%table.table.table-hover
  %thead
    %tr
      %th
        Count
      %th
        Item
  %tbody
    - @prep_sheet.prep_sheet_items.each do |item|
      %tr
        %td
          = item.prep_count
        %td
          = item.par_item.name

.form-actions
  = link_to "Export as CSV", par_sheet_prep_sheet_path(@prep_sheet.par_sheet.id, @prep_sheet.id, :format => :csv), :class => "btn"
  = link_to "Back to all par sheets", par_sheets_url, :class => "btn btn-link"
